# =========================================================
# BOCA Docker Environment Configuration
# =========================================================
# Copy this file to .env and customize the values
# All variables marked as REQUIRED must be set
# =========================================================

# =========================================================
# PostgreSQL Database Configuration
# =========================================================

# Database host (service name in docker-compose)
BOCA_DB_HOST=boca-db

# REQUIRED: Database name
# The name of the PostgreSQL database for BOCA
BOCA_DB_NAME=bocadb

# REQUIRED: Database user (unprivileged)
# This user is used by BOCA application to connect to the database
BOCA_DB_USER=bocauser

# REQUIRED: Database user password
# Password for the unprivileged database user

BOCA_DB_PASSWORD=change_me_bocauser_password

# REQUIRED: Database superuser
# PostgreSQL superuser used for administrative operations
# For standard PostgreSQL images, this should be 'postgres'
BOCA_DB_SUPER_USER=postgres

# REQUIRED: Database superuser password
# Password for the PostgreSQL superuser

BOCA_DB_SUPER_PASSWORD=change_me_postgres_password

# Optional: Database port (default: 5432)
# BOCA_DB_PORT=5432

# Optional: Database encoding (default: UTF8)
# BOCA_DB_ENCODING=UTF8

# Optional: Client encoding (default: UTF8)
# BOCA_DB_CLIENT_ENCODING=UTF8

# Optional: Use local unix socket connection (default: false)
# BOCA_IS_DB_LOCAL=false

# =========================================================
# BOCA Application Configuration
# =========================================================

# REQUIRED: Initial password for system and admin users
# This password is used when BOCA creates the initial 'system' and 'admin' users
# You can change these passwords later via the web interface

BOCA_PASSWORD=change_me_boca_initial_password

# REQUIRED: Secret key for HTTP headers
# Must be a long random string for security
# Generate with: openssl rand -base64 32

BOCA_KEY=change_me_random_secret_key_at_least_20_chars

# Optional: Autojudge host identifier (default: local)
# Used to differentiate between multiple autojudge instances
# BOCA_JAIL_HOST=local

# Optional: Skip safeexec compilation (default: true for dev)
# Set to false in production for better security
BOCA_SKIP_SAFEEXEC=true

# Optional: Enable encryption (default: false)
# BOCA_DO_ENCRYPTION=false

# =========================================================
# Security Recommendations
# =========================================================
# 1. NEVER commit .env file to version control
# 2. Use strong, random passwords for all credentials
# 3. Change default passwords immediately after first deployment
# 4. Use Docker secrets in production instead of environment variables
# 5. Restrict network access to database port (5432)
# 6. Enable SSL/TLS for PostgreSQL in production
# 7. Regularly update BOCA_PASSWORD for system/admin users via web UI
# 8. Consider using a secrets management service (e.g., HashiCorp Vault)
# =========================================================

# =========================================================
# Generate Secure Values
# =========================================================
# Use these commands to generate secure random values:
#
# Database passwords:
#   openssl rand -base64 32
#
# BOCA_KEY:
#   openssl rand -base64 48
#
# Or use a password manager to generate strong passwords
# =========================================================
